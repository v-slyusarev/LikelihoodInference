loadPackage "LikelihoodInference";

-------------------------------------------------------------------------
-- Iterative Proportional Scaling Maximum Likelihood Estimate
-------------------------------------------------------------------------
-- A = matrix({
--   {2,1,0},
--   {1,2,3}
-- });
-- u = {10,20,30};
-- << iterativeProportionalScalingMLE(A, u, err=>0.001) << endl;
-------------------------------------------------------------------------


-------------------------------------------------------------------------
-- Exponential Distribution Random Censoring Problem
-------------------------------------------------------------------------
-- << expRandomCensoringMLE(11,5,7,3, lambdaS => 2) << endl;
-------------------------------------------------------------------------


-------------------------------------------------------------------------
-- Independence Model Maximum Likelihood Estimate
-------------------------------------------------------------------------
-- u = matrix({
--   {1,2,3},
--   {4,5,6},
--   {7,8,9}
-- });
-- << independenceModelMLE(u) << endl;
-------------------------------------------------------------------------


-------------------------------------------------------------------------
-- Saturated Gaussian Model Maximum Likelihood Estimate
-------------------------------------------------------------------------
-- N = saturatedGaussianModelMLE({ {1,2},{-1,-2}, {0,0}});
-- << meanVector N << "    " << covarianceMatrix N << endl;
-------------------------------------------------------------------------


-------------------------------------------------------------------------
-- Rational Parametric Curve Gaussian Model Maximum Likelihood Estimate
-------------------------------------------------------------------------
-- R = QQ[t];
-- N = parametricCurveGaussianModelMLE(
--   {(t+1)^2, t^2},
--   { {1,2}, {-1,-2}, {1,2}}
-- );
-- << meanVector N << "    " << covarianceMatrix N << endl;
-------------------------------------------------------------------------

-------------------------------------------------------------------------
-- Likelihood Ideal
-------------------------------------------------------------------------
-- R = QQ[p_(1,1,1)..p_(2,2,2)];

-- M = multidimensionalMatrix pack(pack(p_(1,1,1)..p_(2,2,2), 2), 2);
-- g = {det M};
-- u = {2, 3, 5, 7, 11, 13, 17, 19};

-- Iu := likelihoodIdeal(g,u);

-- R = QQ[p_(1,1)..p_(2,2)];

-- M = matrix pack(p_(1,1)..p_(2,2), 2);
-- g = {det M};
-- u = {2, 3, 5, 7};

-- Iu := likelihoodIdeal(g,u);

-- <<  Iu << endl;
-------------------------------------------------------------------------